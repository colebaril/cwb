% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_data_tree.R
\name{read_data_tree}
\alias{read_data_tree}
\title{Read all data files from a folder tree safely}
\usage{
read_data_tree(
  path,
  reader,
  cols = NULL,
  ext = NULL,
  include = NULL,
  exclude = NULL,
  recursive = TRUE,
  sheet = NULL,
  sheet_pattern = NULL,
  ...
)
}
\arguments{
\item{path}{Character. Base directory to search for data files.}

\item{reader}{Function. Reader function for the file type (e.g., \code{readr::read_csv}, \code{readxl::read_excel}).}

\item{cols}{Character vector or NULL. Columns to enforce; missing columns will be added as NA.}

\item{ext}{Character vector or NULL. File extensions to include (e.g., c("csv", "xlsx")). Case-insensitive.}

\item{include}{Character vector or NULL. Patterns that must be present in the file path.}

\item{exclude}{Character vector or NULL. Patterns that must NOT be present in the file path.}

\item{recursive}{Logical. Whether to include subfolders. Default TRUE.}

\item{sheet}{Character vector or NULL. Specific sheet names to read (Excel only).}

\item{sheet_pattern}{Character or NULL. Regex pattern to match sheets (Excel only).}

\item{...}{Additional arguments passed directly to \code{reader()}.}
}
\value{
A tibble combining all successfully read files (with columns enforced and source_file added).
}
\description{
Recursively reads multiple data files (CSV, Excel, etc.) from a directory tree.
Automatically handles errors, enforces column names, and combines multiple sheets
from Excel files. Always returns a tibble with consistent columns.
}
